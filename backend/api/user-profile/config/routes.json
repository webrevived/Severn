{
  "routes": [
    {
      "method": "GET",
      "path": "/user-profiles",
      "handler": "user-profile.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/user-profiles/count",
      "handler": "user-profile.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/user-profiles/:id",
      "handler": "user-profile.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/user-profiles",
      "handler": "user-profile.create",
      "config": {
        "policies": ["isMe"]
      }
    },
    {
      "method": "PUT",
      "path": "/user-profiles/:id",
      "handler": "user-profile.update",
      "config": {
        "policies": ["isMe"]
      }
    },
    {
      "method": "DELETE",
      "path": "/user-profiles/:id",
      "handler": "user-profile.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
